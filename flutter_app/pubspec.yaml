name: pneumonia_detector
description: Pneumonia X-ray classifier — Flutter Web + Flask API.

publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  image_picker_for_web: ^3.0.0

flutter:
  uses-material-design: true
